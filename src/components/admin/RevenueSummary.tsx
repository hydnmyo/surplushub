/**
 * SEED STUB — owned by Track D (admin payout, revenue, business model).
 *
 * Total seller fees, total buyer fees and gross platform revenue across orders,
 * broken down by material category. Summary tiles only — charts are Phase 2.
 */
export function RevenueSummary() {
  return (
    <div className="surface-card p-6 text-sm text-muted-foreground">
      The revenue summary is being built. Track D owns this component.
    </div>
  );
}
